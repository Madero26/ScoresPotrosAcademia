<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Academia ITSON</title>
    <link rel="stylesheet" href="resources/styles/styleForms.css">
    <link rel="icon" href="resources/imgs/academia-logo.png">
</head>
<body>
   <%- include('../partials/adminNav', { usuario }) %>
    <h1 class="fotos">FORMULARIO CREAR USUARIO DE COORDINADORES</h1>
    <form action="/crearUsuario" method="POST">
        <label for="usuario">Escribe el usuario:</label>
        <input type="text" name="user"  placeholder="Isma11-12" required>
        <label for="Contraseña">Escriba la Contraseña:</label>
        <input  type="text" name="Contraseña" placeholder="password11-12" required>

        <input type="submit" value="Subir">
    </form>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if(typeof alert !="undefined" ) { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: <%= showConfirmButton %> ,
                timer: <%= timer %>
            }).then(() => {
                    window.location = '/<%= ruta %>'
                })
        </script>

        <% } %>
</body>
</html>